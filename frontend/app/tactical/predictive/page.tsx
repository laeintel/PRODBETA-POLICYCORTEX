'use client';
import TacticalPageTemplate from '../../../components/TacticalPageTemplate';
import { LineChart } from 'lucide-react';

export default function Page() {
  return <TacticalPageTemplate title="Predictive Analytics" subtitle="Predictive Analytics Operations Center" icon={LineChart} />;
}