'use client';
import TacticalPageTemplate from '../../../components/TacticalPageTemplate';
import { CheckCircle } from 'lucide-react';

export default function Page() {
  return <TacticalPageTemplate title="Availability Tracking" subtitle="Availability Tracking Operations Center" icon={CheckCircle} />;
}