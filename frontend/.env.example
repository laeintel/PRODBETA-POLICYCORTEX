# Azure AD Configuration
# For production, configure your Azure AD app as SPA and add these values
NEXT_PUBLIC_AZURE_CLIENT_ID=your-client-id-here
NEXT_PUBLIC_AZURE_TENANT_ID=your-tenant-id-here

# Demo Mode - Set to true ONLY for testing without Azure AD
# WARNING: This bypasses authentication and should NEVER be true in production
NEXT_PUBLIC_DEMO_MODE=false

# Backend API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:4000/graphql

# Optional: MSAL Redirect URIs
NEXT_PUBLIC_MSAL_REDIRECT_URI=http://localhost:3000
NEXT_PUBLIC_MSAL_POST_LOGOUT_REDIRECT_URI=http://localhost:3000

# Environment
NODE_ENV=development

# For production deployments, ensure:
# 1. NEXT_PUBLIC_DEMO_MODE is set to false
# 2. Valid Azure AD credentials are configured
# 3. NODE_ENV is set to production