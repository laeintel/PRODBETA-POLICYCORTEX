apiVersion: apps/v1
kind: Deployment
metadata:
  name: core
  namespace: policycortex-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: core
  template:
    metadata:
      labels:
        app: core
    spec:
      containers:
      - name: core
        image: ACR_NAME.azurecr.io/core:IMAGE_TAG
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: policycortex-config
        env:
        - name: RUST_LOG
          value: "info"
        - name: SQLX_OFFLINE
          value: "true"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: core-service
  namespace: policycortex-dev
spec:
  selector:
    app: core
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP